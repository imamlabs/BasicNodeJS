<section class="text-center">
    <div class="card mx-4 mx-md-5 shadow-5-strong" style="
            margin-top: 60px;
            background: hsla(240, 23%, 95%, 0.8);
            backdrop-filter: blur(30px);
            ">
        <div class="card-body py-7 px-md-7">
            <div class="row  justify-content-center">
                <div class="container">
                    <h1><%= title %></h1>
                    <div class="table-responsive">
                        <table class="table table-bordered table-hover">
                            <thead class="table-primary">
                                <tr>
                                    <th scope="col">No</th>
                                    <th scope="col">Username</th>
                                    <th scope="col" style="width: 20%">Aksi</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% let count = 1; %>
                                <% for(let useradmin of usersadmin) { %>
                                   
                                        <tr class="table-row" data-bs-toggle="tooltip" data-bs-placement="top" title="Klik untuk detail">
                                            
                                            <th scope="row"><%= useradmin.id %></th>
                                            <th scope="row"><%= useradmin.username %></th>
                                            <td>
                                                <a href="/editadmin/<%= useradmin.id %>" class="btn btn-primary btn-sm btn-block custom-width">Edit</a>
                                                <!-- Tambah tombol Edit -->
                                                <form action="/hapusadmin/<%= useradmin.id %>" method="post" style="display:inline;">
                                                    <button type="submit" class="btn btn-danger btn-sm btn-block custom-width">Hapus</button>
                                                    <!-- Tambah tombol Hapus -->
                                                </form>
                                            </td>
                                        </tr>
                                   
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <br>
            <br>
        </div>
    </div>
</section>
